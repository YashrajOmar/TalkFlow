# Unrelated workspace projects
campus-core/
web/
yashraj-portfolio/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/

# Whisper / Ollama model caches (can be gigabytes)
local-transcriber/.cache/
local-transcriber/models/
~/.cache/huggingface/

# Environment configurations
.env
.env.local
.env.*.local

# IDEs and editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs, temp files, databases
*.log
local-transcriber/temp_audio/
*.db
*.sqlite
*.tmp

# Audio recordings — never commit user audio
*.webm
*.wav
*.mp3
*.ogg
*.mp4
*.flac
*.m4a

# OAuth secrets — never commit these
# Replace the placeholder client ID in manifest.json with your own;
# do not hardcode real credentials in source files.
oauth_secret.json
client_secret*.json

# Native Messaging Host — generated installer artifacts
local-transcriber/com.talkflow.local.installed.json
local-transcriber/native_host_runner.bat
local-transcriber/native_host.log

# PyInstaller output
dist/
build/
*.spec

# Node / Web
node_modules/
dist/
build/

# OS metadata
.DS_Store
Thumbs.db
