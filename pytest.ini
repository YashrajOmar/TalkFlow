[pytest]
markers =
    live: tests that require the local TalkFlow server to be running on port 8765
    offline: tests that require the local server to be STOPPED
    slow: tests that may take 10+ seconds (model pulls, server boot)
